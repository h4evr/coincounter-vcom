PROJECT(coindetector-vcom)
FIND_PACKAGE(OpenCV REQUIRED)
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
ADD_EXECUTABLE(coindetector main.cpp)
TARGET_LINK_LIBRARIES(coindetector ${OpenCV_LIBS})
